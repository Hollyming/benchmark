input_path: examples/seed_documents.jsonl
output_path: examples/generated/seed_corpora_ingestion/source_documents.jsonl
redact_private: true
optional_public_data:
  enabled_by_default: false
  staging_dir: data/raw
  notes:
    - "Download public corpora manually into data/raw/ after reviewing licenses."
    - "Convert each source to JSONL rows with doc_id, title, text, created_at, origin, uri, license."

